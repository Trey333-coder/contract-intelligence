{
  "name": "atropa-contract-intelligence-bot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Read-only PulseChain contract discovery with ABI-first selector and Unicode intelligence for Atropa.",
  "scripts": {
    "scan": "node scripts/scan.mjs",
    "analyze": "node scripts/analyze.mjs",
    "build": "node scripts/build-site.mjs",
    "all": "node scripts/scan.mjs && node scripts/analyze.mjs && node scripts/build-site.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
