{
  "name": "atropa-contract-intelligence-bot",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Read-only Atropa contract intelligence, compact UI, Unicode/ABI analysis, and live workbook data.",
  "scripts": {
    "scan": "node scripts/scan.mjs",
    "analyze": "node scripts/analyze.mjs",
    "build": "node scripts/build-site.mjs",
    "all": "node scripts/scan.mjs && node scripts/analyze.mjs && node scripts/build-site.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test test/*.test.mjs",
    "live": "node scripts/atropa-live-pull.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
